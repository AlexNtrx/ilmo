# RR-01 Worklog
- Status: Implementation approved; commit pending
- Branch: `feature/rr-01-release-readiness`
- Base: `4d070cbb9b63849cf66e45f2f16fe38f332975fb`
- Planned commit: `chore(release): complete version 0 readiness review`

## Decisions
- `/` redirects to `/staff`; the existing auth boundary owns login routing.
- The login title is the semantic page `h1` without visual changes.
- Local records are excluded from release and cannot be deleted by inference.
- Production must use a clean hosted PostgreSQL database.
- Category cards retain ordering controls but hide internal `sortOrder` values.
- The approved result is Ilmo Version 0 release candidate, not production ready.
- Confirmed BLOCKER, IMPORTANT, and MINOR defects remaining: zero.
- The owner accepts the remaining NOT RUN checks as verification limitations.

## Changed Files
- Replaced the scaffold root with the approved server redirect.
- Corrected login heading semantics and Finnish reference-count grammar.
- Added a focused count formatter test.
- Synchronized project, workflow, progress, and release documentation.

## Validation
- PASSED: 51 unit tests, authenticated staff-role integration, lint, build,
  `git diff --check`, and 30 internal Markdown links.
- PASSED: unauthenticated `/` reaches `/staff/login` without a loop; the login
  `h1` and public/login layouts have no overflow at 360, 390, 768, or 1280 px.
- PASSED: D05 through the admin UI; the public form shows six active Categories
  in order. Tab, Enter, Space, Escape, focus trap/restoration, Cancel without
  mutation, one success toast, visible 20-second polling, and cleanup passed.
- PASSED: authenticated desktop pages had no overflow, clipped Finnish copy,
  console errors, or error overlays.
- NOT RUN: authenticated responsive checks at exactly 360, 390, 768, and 1280 px.
- NOT RUN: visually observing the pending lock under a deliberately slow mutation.
- NOT RUN: hidden-tab polling pause.
- NOT RUN: immediate polling refresh after returning to the tab.
- NOT RUN: open-dialog preservation during background refresh.
- NOT RUN: scroll preservation with sufficient scrollable dashboard content.

## Unresolved Risks
- Better Auth build warnings remain when URL and secret are unavailable.
- Exact authenticated mobile and hidden-tab verification depends on browser
  control support that can reproduce those states.
- Unknown local development records remain intentionally untouched.
