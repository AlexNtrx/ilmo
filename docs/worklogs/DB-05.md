# DB-05 Worklog
- ID: DB-05
- Title: Initial Migration, Seed, and Database Verification
- Status: Implemented and validated; approval pending
- Branch: `feature/db-05-migration-seed`
- Base commit: `82ac7893c2e86c01d2be9ce6217544b83ae10fe3`
- Planned commit: `feat(database): add initial migration and seed`

## Decisions
- Seed the six approved Finnish categories only when the table is empty.
- Upsert `pilot-wc-001` without updating an existing Location.
- Provision the first admin through Better Auth only when no admin exists.
- Validate all required admin inputs before any seed write.

## Changed Files
- Added `prisma/seed.ts` and `prisma/verify.ts`.
- Added seed configuration, scripts, placeholders, and exact `tsx`.
- Added and applied `20260730184652_init`; schema stayed unchanged.
- Synchronized the approved project documentation.

## Validation
- Prisma format, validation, and Client generation: passed.
- Read-only PostgreSQL preflight against `ilmo`: passed.
- First create-only: failed with `spawn UNKNOWN`; approved retry passed.
- SQL review, migration apply, status, and lint passed.
- Sandbox build failed on fonts; the network build passed.
- Invalid seed inputs failed before writes; corrected seed passed.
- Second seed created no records; full verification passed twice.

## Unresolved Risk
- Schema-engine execution was intermittent and remains a Windows risk.
- Better Auth reported that `BETTER_AUTH_URL` was not visible at runtime.
